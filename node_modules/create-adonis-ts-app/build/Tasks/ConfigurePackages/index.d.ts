import { TaskFn } from '../../src/contracts';
/**
 * Configure installed packages by running `node ace invoke instructions`
 * on all of them in sequence
 */
declare const task: TaskFn;
export default task;
