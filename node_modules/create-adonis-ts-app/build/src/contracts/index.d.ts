import { logger as sinkLogger, files } from '@adonisjs/sink';
import { ApplicationContract } from '@ioc:Adonis/Core/Application';
/**
 * Shape of task functions
 */
export declare type TaskFn = (application: ApplicationContract, logger: typeof sinkLogger, state: CliState) => void | Promise<void>;
/**
 * CLI state
 */
export declare type CliState = {
    baseName: string;
    absPath: string;
    client: 'yarn' | 'npm';
    boilerplate: 'web' | 'api';
    projectName: string;
    eslint: boolean;
    prettier: boolean;
    pkg: files.PackageJsonFile;
};
