import { CliState } from '../contracts';
/**
 * Returns the state for creating project
 */
export declare function getState(projectRoot: string, options: {
    boilerplate?: 'web' | 'api';
    projectName?: string;
    eslint?: boolean;
    prettier?: boolean;
    client: 'yarn' | 'npm';
}): Promise<CliState>;
/**
 * Log installing dependencies message
 */
export declare function getInstallMessage(list: string[]): string;
/**
 * Find if process is a child process of yarn or not
 */
export declare const usingYarn: boolean;
